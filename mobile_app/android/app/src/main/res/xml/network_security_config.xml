<!--
  ShopZen network security policy.
  - Production API is HTTPS (dukaanflow.onrender.com) and is unaffected.
  - Cleartext HTTP is permitted ONLY for private LAN ranges so a developer
    phone can talk to a Django dev server on the local Wi-Fi (e.g. 192.168.x.x).
    Public internet cleartext remains blocked.
  Remove the <domain-config> block for store-distributed release builds.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">10.0.0.0</domain>
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">192.168.0.0</domain>
        <domain includeSubdomains="true">192.168.1.0</domain>
        <domain includeSubdomains="true">192.168.2.0</domain>
        <domain includeSubdomains="true">172.16.0.0</domain>
        <domain includeSubdomains="true">localhost</domain>
    </domain-config>
</network-security-config>
