// ShopZen v2 is a local-first application: the admin payment-verification
// panel was part of the cloud subscription system and has no local
// equivalent (there is no server to verify against). The screen and
// providers were removed; the server-side code still exists for the
// DukaanFlow web app.
