# Pinned to the exact versions verified working on 2026-09-06.
# Upgrade deliberately: change one line, run the full backend test suite,
# then deploy. Never leave production ranges unpinned.
Django==6.1.1
djangorestframework==3.18.0
djangorestframework-simplejwt==5.5.1
django-cors-headers==4.9.0
dj-database-url==3.1.2
python-dotenv==1.2.3
gunicorn==26.2.0
whitenoise==6.12.0
Pillow==12.3.0
reportlab==5.0.1
# Production PostgreSQL driver (Render/Heroku style DATABASE_URL)
psycopg2-binary==2.9.11
